# Example environment variables for the server
# Never commit secrets to source control. Use a secret manager or GitHub Actions Secrets in CI.

PORT=4000
APP_API_KEY=replace-with-your-api-key
CORS_ORIGIN=http://localhost:5173
